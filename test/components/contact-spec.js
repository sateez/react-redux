import {renderComponent , expect} from '../setup'
import ContactForm from '../../containers/ContactForm'

describe ('Contacts form',() =>{
 it('Check for contact form existance', () => {
   expect(true).to.equal(true)//comes in E2E part
 })
})
