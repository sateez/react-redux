import {renderComponent , expect} from '../setup'
import DetailsForm from '../../containers/DetailsForm'

describe ('Details form',() =>{
 it('Check for details form existance', () => {
   expect(true).to.equal(true) //comes in E2E part
 })
})
